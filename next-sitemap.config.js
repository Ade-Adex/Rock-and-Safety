/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.rockandsafety.com',
  generateRobotsTxt: true, // Automatically creates robots.txt as well
  // (Optional) add paths to exclude from sitemap
  // exclude: ['/admin/*'],
}
